# Thus.Live Editor 0.1.0 验证记录

验证环境：Windows x64，Electron 44.4.3。代码和构建产物位于独立的编辑器目录。

## 已通过

- TypeScript / Vue 类型检查与生产构建。
- 14 项核心测试：项目检测不执行配置脚本；草稿索引；保存版本冲突；路径越界、日期与重名校验；YAML 注释与未知字段保留；图片重名与只读限制；移动时修复引用；千篇文章索引；扩展渲染与预览文件访问边界；稳定版本标签解析与语义版本比较。
- 4 项真实 Electron 窗口端到端流程，覆盖连接项目、文章与首页编辑、公式与图表预览、代码组切换、保存与外部冲突、元信息、明暗主题、快速打开、离开保护、恢复副本、新建草稿、图片导入、文章移动、搜索筛选、项目设置以及开发版/构建版界面回归。
- GitHub 最新稳定 Release 检查已验证；网络失败保持静默，不阻塞本地编辑。“不再提示”只记录当前检查到的版本，后续更高稳定版本仍会提醒。
- 打包后的 Windows 程序启动及项目读取检查。
- Thus.Live 原项目以只读模式读取，其主题与正文样式正常加载，配置检测无警告；原项目 Git 工作区仍为空。

## 测试数据隔离

所有新建、保存、导入图片和移动测试都使用临时项目。真实 Thus.Live 项目只读取，不写入文章、配置、依赖或构建目录。编辑器配置及恢复副本也使用独立的测试目录。

## 尚需对应环境验证

macOS、Linux 已提供构建目标与 CI 流程，但本机不能代替这两个系统的实际运行验证。发行签名、公证以及 Linux 各发行版桌面集成需在发行阶段完成。

本版是可运行的首版。未支持的动态 Vue 组件、外部片段引入及复杂链接自动重写边界，见 README 的「预览与兼容范围」。
